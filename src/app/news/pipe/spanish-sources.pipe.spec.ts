import { SpanishSourcesPipe } from './spanish-sources.pipe';

describe('SpanishSourcesPipe', () => {
  it('create an instance', () => {
    const pipe = new SpanishSourcesPipe();
    expect(pipe).toBeTruthy();
  });
});
